Tesi su tirocinio "Sviluppo ed infrastruttura web con AWS" presso Soluzioni Futura

Buongiorno prof,
le vorrei chiedere come procedere per il lavoro di tesi al seguito del tirocinio curriculare, per il quale ho ottenuto l'idoneità con l'opzione di tesi.
Il tema del report era "Sviluppo ed infrastruttura web con AWS", riporto un sintetico table of contents:

1) Impostazione di una Virtual Private Cloud
2) Utilizzo di EC2 (server linux)
3) Configurazione di Nginx (server web) e Launch Configuration (configurazioni automatiche)
4) Utilizzo di API AWS in Node.js
5) Pipeline di CI/CD (da push su una repository remota ad aggiornamento dei server in automatico)
6) Stesura di template di Cloudformation (configura i servizi di cui un cliente ha bisogno in maniera automatica)
7) Progetto principale: workflow automatico di Github Actions che permette di attivare Cloudformation. Quando un cliente di Soluzioni Futura avvia una repository con inserisce dei files di configurazione e dei secrets, può configurare alcuni parametri di interesse (capacità database, velocità di calcolo...) per la pubblicazione del progetto nella cloud. Ad ogni creazione di branch della repository viene avviato un ambiente cloud isolato di testing con anche configurazioni diverse, utile per fare presentazioni di versioni alpha.

Le chiedo se c'è da fare un'estensione per il documento di tesi, ed in quel caso su quali argomenti. Ultimamente sto approfondendo una architettura per applicazioni web con pattern mvc e le seguenti tecnologie: Express (usando pattern middleware), TypeORM (un framework orm che supporta la tipizzazione statica di Typescript), Nuxt (un framework frontend che supporta server-side-rendering ed hydration con Vue.js), magari potranno essere utili anche per la tesi.

Grazie per l'attenzione, buona giornata!

PS: mi scuso se la email precedente (quella riguardante la presentazione dei tirocini curriculari fatta su Teams) la ho inviata due volte, ho avuto problemi a configurare un client email 😅.
